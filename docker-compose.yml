services:
  midware:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    environment:
      HOST: "0.0.0.0"
      PORT: "${PORT:-5000}"
      MIDWARE_SECRET_KEY: "${MIDWARE_SECRET_KEY:?set MIDWARE_SECRET_KEY}"
    volumes:
      - ./appdata:/data
